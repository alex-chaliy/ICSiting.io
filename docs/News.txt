News

- /home - главная страница
- /news - страница новостей

! Новости на главной странице
	  формируются так:
	  - берутся 2 последних поста с тегом 'ics' и выводятся в левый столбик
	  - берутся 2 последних поста с тегом 'it' и выводятся в правый столбик

! Новости на странице новостей
	  формируются так:
	  - берутся все посты
	  - выводятся

Коментов пока не будет.
Далее будет возможность поделиться в соц. сетях